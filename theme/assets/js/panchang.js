/* Panchang page — geolocation enhancement (optional) */
(function () {
    'use strict';

    document.addEventListener('DOMContentLoaded', function () {
        // City <select> auto-submits via inline onchange — no JS needed for that.

        // Optional: "Use my location" button could be added in future
        // Browser geolocation → reverse-geocode → match closest city
    });
})();
